# 嵌入式故障调试详细报告

> 必须依据对话、源码改动、命令输出和日志中的真实证据生成。未知信息明确标记“未提供”或“未验证”，禁止猜测、补造日志或把编译成功等同于故障解决。

## 1. 报告摘要

| 项目 | 内容 |
|:---|:---|
| 报告标题 | `{short_desc}` |
| 工程名称/路径 | `{project_name}` / `{project_dir}` |
| 故障状态 | 已解决 / 部分解决 / 未解决 / 仅完成静态分析 |
| 调试时间范围 | `{start_time}` ～ `{end_time}` |
| 构建验证模式 | full / compile_only / none |
| 最终结论 | 用 2～4 句概括现象、根因、修复和验证边界 |

## 2. 用户诉求与对话背景

- 用户原始问题：保留关键原意，不虚构逐字引语。
- 预期行为：说明设备或软件在正常情况下应如何工作。
- 实际行为：说明可观测到的异常。
- 触发条件：操作步骤、输入、连接方式、时序和环境。
- 发生频率：必现 / 偶现 / 未知。
- 对话补充信息：记录用户后续确认、否定和约束。

## 3. 环境与工程配置

| 项目 | 实际值 |
|:---|:---|
| 工程文件 |  |
| MCU / 硬件版本 |  |
| Keil 路径 / 构建目标 |  |
| 串口参数 | 端口、波特率、数据位、停止位、校验位 |
| 下载器 | 类型、接口或端口 |
| Git 分支 / 基线提交 |  |
| 相关固件版本 |  |

## 4. 故障现象与复现步骤

### 4.1 故障现象

详细描述错误码、状态值、异常日志、数据差异及设备行为。

### 4.2 复现步骤

1. 准备条件。
2. 操作或输入。
3. 触发动作。
4. 观察点及判定标准。

### 4.3 影响范围

- 受影响模块、接口和设备。
- 是否影响通信、定时器、中断、外设或其他工程。
- 风险等级和用户影响。

## 5. 排查过程时间线

| 序号 | 假设 / 目标 | 执行动作 | 证据与结果 | 结论 / 下一步 |
|:---:|:---|:---|:---|:---|
| 1 |  |  |  |  |

> 必须记录关键失败尝试、被排除的假设以及每轮 CHESHI 观测；不得只写最终成功路径。

## 6. 根因分析

### 6.1 直接原因

指出具体文件、函数、状态机、协议字段或硬件条件，以及异常如何被触发。

### 6.2 根本原因

解释设计、边界检查、并发时序、配置或流程层面的根因。

### 6.3 证据链

1. 现象证据：原始日志或用户确认。
2. 代码证据：相关文件、符号和逻辑。
3. 实验/对照证据：修改前后观测差异。
4. 排他证据：为何排除其他主要假设。

## 7. 修复方案与代码变更

### 7.1 修复策略

说明为何选择当前方案、替代方案及取舍。

### 7.2 修改文件清单

| 文件路径 | 符号 / 区域 | 修改内容 | 修改原因 | 风险 |
|:---|:---|:---|:---|:---|
| `xxx.c` | `function_name` |  |  |  |

### 7.3 修改原理

详细说明数据流、状态迁移、边界条件、并发/中断安全性及协议兼容性。

## 8. 验证过程与结果

| 验证项 | 方法 / 命令 | 预期结果 | 实际结果 | 状态 |
|:---|:---|:---|:---|:---:|
| 编译 |  | 0 Error |  | ✅/❌/未执行 |
| 下载 |  | Verify OK |  | ✅/❌/未执行 |
| 故障复现 |  | 原故障不再出现 |  | ✅/❌/未执行 |
| 回归检查 |  | 相关功能正常 |  | ✅/❌/未执行 |

### 8.1 关键日志证据

```text
粘贴必要且精简的原始日志，并说明其时间、来源和含义。
```

### 8.2 验证边界

- 未执行的测试及原因。
- `compile_only` / `none` 模式必须明确写明“未完成板端验证”。
- 验证窗口、样本数量和不能覆盖的偶发场景。

## 9. 回归风险与后续建议

- 潜在回归点及监控方法。
- 建议补充的单元、集成、长稳或边界测试。
- 需要同步修改的关联工程、文档或配置。
- 遗留问题、负责人和建议优先级（未知则注明）。

## 10. 结论

明确说明故障是否解决、依据是什么、还有哪些未验证事项。不得使用无证据的“完全正常”。

## 11. 附件与引用

- 配置文件：`{config_path}`
- 构建日志：`{build_log_path}`
- 调试日志：`{debug_log_path}`
- 验证日志：`{verify_log_path}`
- 相关提交：`{commit_refs}`

---

*报告生成时间：`{generated_at}`*
*调试工具：embedded-debug-workflow Skill*
