# 强制规则（AI 行为约束，优先级仅次于流程门禁）

> 本文件规定 AI 的**行为约束**（交互、Git、宏、路径、迭代上限）。
> 流程的**顺序与门禁纪律**（必须按步骤顺序执行、不可跳过门禁、以引擎 JSON 为准）由 `flow.yaml` 的步骤表 + `SKILL.md` 铁律统一规定，二者互补、不重复。

1. **交互询问与进度展示规则**

1.1 — seq 1 的 `config_reader.py --interactive` 在终端中使用数字完成全部初始化参数选择：配置版本 `1=按当前配置进行`、`2=已修改配置按照最新进行`；每个项目模式 `1=none`、`2=compile_only`、`3=compile_flash`、`4=full`。脚本自行重新读取、校验并保存结果，引擎自动同步状态。旧的 AI 配置确认和项目模式询问步骤不再兼容，AI 禁止重复提问。seq 2 只快速审查相关源码和历史；seq 3 使用 agent 提问工具采集故障信息。运行中仅固定阻塞场景触发 `🛑人工暂停`。

1.2 当配置 `ai_progress_display=true` 时，仅在每次调用引擎之前发送独立进度消息，格式严格为 `> 内容`（行首一个 `>` 加一个空格），内容包括上一次动作的简要结果和即将调用的步骤；首次调用输出 `> 初始化流程，准备进入步骤 1`。引擎返回后禁止输出任何步骤，不得为进度增加引擎调用。配置为 `false` 时禁止输出调用前流程进度。进度不得扩写为分析、根因、日志或结论汇报。暂停场景细则见 `refs/pause-scenarios.md`。

2. **Git 版本管控规则** — 全程禁止 `git push`，所有分支/提交/回退仅在本地操作；每一轮修改前必须先提交当前状态建立回退基线，之后再改源码或配置；临时 CHESHI 代码与正式业务修复代码强制隔离。详见 `refs/git-rules.md`。

3. **CHESHI 统一宏规范** — 全局仅使用 `CHESHI` 单一调试总开关（Bit 位掩码或数值分级）；凡调试结束需要删除的新增代码均须完整包裹，包括头文件引用、宏、类型/声明、变量、参数、辅助函数、采集器、缓冲区、Flush 及调用路径，不得只包裹打印。通信层/ISR 仅采集快照，由 `main` 主循环输出；宏定义集中写在 `main.c` 头部，调试结束整段删除并恢复临时工程配置。详见 `refs/cheshi-macro.md`。
   额外约束：插入 CHESHI/调试日志不是每轮强制动作，由 AI 根据当前故障阶段、是否能产出新证据以及是否对定位根因有帮助来裁决。编译失败、语法错误、工程/配置问题、硬件不可用等场景下，优先修复问题或收敛条件，不应盲目插日志。

4. **动态路径加载规则** — 流程引擎的 `--project` 为历史兼容参数名，必须传当前 VS Code **工作区根目录**，不得传 Skill 仓库目录或某个 Keil 工程目录。工作区配置、状态、日志和报告的路径必须由 `scripts/path_config.py` 根据 `scripts/skill-config.json` 生成，禁止在 Python、YAML 或 Markdown 中重复拼接；实际源码目录和工程文件从配置的 `projects[*]` 解析。日志文件必须包含项目下标和名称，禁止多项目覆盖。Keil 默认路径由集中配置自动填入，不询问用户。详见 `refs/config-format.md` 和 `refs/runtime-config.md`。

5. **8 轮迭代上限** — 自动加打印满 8 轮仍无法定位故障时，必须触发人工求助。详见 `refs/debug-loop.md`。

6. **持续运行规则（运行中不可随意停下）** — 流程一旦进入执行，AI 必须持续驱动引擎按 `seq` 推进，**非必要情况一律不得停下/退出流程**：
   - **非必要情况（禁止停下）**：需要修改代码、需要向用户询问、需要等待确认、某一步暂未定位等——这些本就是流程内的正常环节（`edit_source` / `ask_user` / 迭代回退），必须在线内完成，不得借机中止整个流程或要求用户手动接管。
   - **必要情况（允许 `🛑人工暂停`）**：仅限真正阻断流程、AI 无力绕过的硬阻塞，例如：
     1. 程序卡死、目标板死机/看门狗失效，必须断电重启才能继续；
     2. 硬件缺失或不可用（无目标板、无下载器、串口/调试器丢失、供电异常），流程客观上无法推进。
  - 除以上必要情况外，AI 不得主动停下流程；即便需要用户补充信息，也应走流程内的 `ask_user` 步骤（如 seq 13 追问环节），而非终止运行。必要暂停场景细则见 `refs/pause-scenarios.md`。

7. **编译下载与串口监听由 AI 决策** — 禁止询问用户是否设置 `skipBuild` / `skipFlash`。用户或 AI 希望开始时先观察板上现有固件串口时，自动跳过编译下载并启用 `observeExistingSerial`；每轮应根据当前证据设置 `serialCaptureRequested`，只有需要串口证据时才监听，不能因为项目模式为 `full` 就强制读取窗口。用户明确要求结束流程时自动启用 `finishRequested`，编译失败只能作为报告中的未验证项，不得阻塞 CHESHI 清理、报告生成和流程完成。

8. **报告步骤失败处理（step 20）** — 报告步骤失败时进入人工暂停（`wait_user`），**不自动重试覆盖**；如需重新生成或修正报告内容，处理完使用 `--wake` 恢复后继续。禁止 `on_failure` 无条件跳转回同一步形成死循环（与 Test Terminator 同源反模式一致）。
